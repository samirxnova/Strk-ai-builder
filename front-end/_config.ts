const config = {
  metadata: {
    title: 'Strk AI Builder',
    // eslint-disable-next-line quotes
    description: "Strk AI Builder, to build through intelligence",
    keywords:
      'react.js, react.js-template, react.js-boilerplate, typescript, shadcn/ui, tailwindcss, vite, swc, eslint, prettier, husky, commitlint, lint-staged'
  },
  server: {
    host: 'localhost',
    port: 3000
  }
};

export default config;
